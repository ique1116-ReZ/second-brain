# 第二大脑系统部署指南

## 🚀 Vercel 部署步骤（完全免费）

### 方法一：通过 GitHub 部署（推荐）

1. **创建 GitHub 仓库**
   - 访问 https://github.com/new
   - 仓库名: `second-brain`
   - 选择 Public（公开）
   - 点击 "Create repository"

2. **推送代码到 GitHub**
   ```bash
   # 在本地电脑上操作
   git remote add origin https://github.com/你的用户名/second-brain.git
   git push -u origin main
   ```

3. **部署到 Vercel**
   - 访问 https://vercel.com/new
   - 点击 "Import Git Repository"
   - 选择你的 `second-brain` 仓库
   - 点击 "Deploy"
   - 等待部署完成（约1-2分钟）

4. **访问你的网站**
   - 部署完成后会获得一个免费域名：`https://second-brain.vercel.app`
   - 可以自定义域名（免费）

### 方法二：通过 Vercel CLI 部署

1. **安装 Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **登录 Vercel**
   ```bash
   vercel login
   ```

3. **部署项目**
   ```bash
   cd second-brain
   vercel
   ```

4. **生产环境部署**
   ```bash
   vercel --prod
   ```

## 🌐 免费功能

Vercel 免费层包含：
- ✅ 无限部署
- ✅ 自定义域名（免费）
- ✅ 自动 HTTPS/SSL
- ✅ 全球 CDN
- ✅ 自动 CI/CD
- ✅ 100GB 带宽/月
- ✅ 永远免费（个人项目）

## ⚙️ 环境配置

项目已配置为开箱即用，无需额外配置。

## 🔧 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **图标**: Lucide React
- **部署**: Vercel

## 📁 项目结构

```
second-brain/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 主页
│   └── globals.css        # 全局样式
├── components/            # React 组件
│   ├── command-menu.tsx   # 全局搜索 (Cmd+K)
│   ├── memory-list.tsx    # 记忆列表
│   ├── filters.tsx        # 多维筛选
│   ├── stats.tsx          # 统计面板
│   ├── search-button.tsx  # 搜索按钮
│   └── global-search.tsx  # 搜索上下文
├── lib/                   # 工具函数
│   └── data.ts           # 模拟数据
├── types/                 # TypeScript 类型
└── public/               # 静态资源
```

## 🎯 核心功能

1. **全量列表展示** - 支持搜索的记忆点列表
2. **全局搜索 (Cmd+K)** - 快捷键唤起的全局搜索
3. **多维筛选** - 按类型、日期、标签筛选
4. **极致简约 UI** - Minimal Design 设计风格
5. **响应式设计** - 适配所有设备

## 🔄 后续开发

### 本地开发
```bash
npm install
npm run dev
# 访问 http://localhost:3000
```

### 构建生产版本
```bash
npm run build
npm start
```

### 更新部署
推送代码到 GitHub 后，Vercel 会自动重新部署。

## 📞 支持

如有问题：
1. 检查控制台错误信息
2. 查看 Vercel 部署日志
3. 联系开发者

---

**部署完成后的访问地址：** `https://second-brain.vercel.app`

**系统默认包含：**
- 10个示例记忆点
- 完整的搜索和筛选功能
- 响应式设计
- 暗色/亮色模式支持